<div class="module module--list popular-stories module--sidebar">
    <h3 class="hd-g module__heading">{{ header }}</h3>

    <ul class="story-list cf {{ class }}">
        {% for  post in posts %}
            <li>
                <article class="story story--link">
                    <a href="{{ post.permalink }}" class="link-wrapper">
                        <header class="story__header">
                            <h4 class="hd-h story__hed">{{ post.post_title }}</h4>
                            <span class="byline">by {{ post.author.name }}</span>
                        </header>
                    </a>
                </article>
            </li>
        {% endfor %}
    </ul>
</div>